# Runtime Data

This folder is used for local runtime storage.

- `storage.json`: optional one-time legacy import source
- `workspaces/`: generated room workspace snapshots for terminal and code execution

The folder stays in the repo only as a known location. Runtime contents are ignored and should not be committed.

# Copy this file to `client/.env.local` when you need local overrides.
# Optional when the frontend is served by FastAPI from the same domain.
# Set this only if you deploy the frontend separately from the API.
VITE_API_URL=https://your-api.example
VITE_GOOGLE_CLIENT_ID=
